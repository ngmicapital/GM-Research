# READINESS_MANIFEST — reproducibility receipt

The receipt that lets Fable trust (or re-check) this package. If any of this no longer matches the
repo, re-derive before acting.

## Repo state (when the artifacts were generated)

| Field | Value |
|-------|-------|
| Base commit (artifacts generated against) | `d12eb0d` — "Recover 2026-07-01 praxis brief + health-check table fixes" |
| Branch | `main` |
| `origin/main` at snapshot | `18a80e9` (base commit `d12eb0d` was local-only at snapshot; pushed with this package) |
| This package | committed as the next commit on top of `d12eb0d` (its own SHA can't self-reference; see git log) |
| Node (local) | v24.14.1 · CI pins Node 20 — both satisfy the repo's `≥20`. Minor drift, no known impact. |
| Snapshot time | 2026-07-02 (AEST) / 2026-07-01T16:34Z |
| Working-tree dirty state at snapshot | only `readiness/` untracked (the package itself); no other uncommitted changes |

## Artifacts — path · how generated · replay status

| Artifact | How it was generated | Replay status |
|----------|----------------------|---------------|
| `CURRENT_STATE.md` | Hand-written by Opus from verified repo facts | JUDGMENT (not a metric) |
| `GOAL.md` | Hand-written by Opus in owner's voice from dc's 2026-07-02 scope decision (mandate = full product upgrade) | JUDGMENT — dc to confirm voice/priorities |
| `TRUTH_TABLE.md` | Hand-written; each row carries its own re-derivation command | Every row RE-RUNNABLE from repo root (see the table) |
| `MODULE_MAP.md` | Generated by a Sonnet subagent via grep-verified wiring; spot-checked by Opus (critical-path + the CI-test-gap claim confirmed against `ci.yml`) | RE-RUNNABLE (grep the cited references) |
| `EVOLUTION.md` | Hand-written by Opus from `git log` (1,414 commits, cited SHAs) | RE-RUNNABLE (`git show <sha>`) |
| `EVIDENCE/failure-forensics.md` | Hand-written by Opus from `fix(...)` commits + health-check | RE-RUNNABLE (cited SHAs + `health-check.js`) |
| `EVIDENCE/tests.out.txt` | `node --test scripts/lib/lib.test.js scripts/lib/render.test.js` | **RAW-REPLAYABLE** — re-run the command |
| `EVIDENCE/generate-index.out.txt` | `node scripts/generate-index.js` | **RAW-REPLAYABLE** |
| `EVIDENCE/generate-visualizations.out.txt` | `node scripts/generate-visualizations.js` | **RAW-REPLAYABLE** |
| `EVIDENCE/health-check.out.txt` | `node scripts/health-check.js` (also writes `health-reports/2026-07-01.json`) | **RAW-REPLAYABLE** |
| `EVIDENCE/repo-state.txt` | git snapshot commands | **RAW-REPLAYABLE** |
| `FABLE_MODE.md` / `ONESHOT_PROMPT.md` | Hand-written by Opus | JUDGMENT |
| `archive/2026-06-24-market-briefing.CODEX-VARIANT.html` | `git show codex/briefings-run:...` | Preserved divergent variant (NOT adopted) |
| `archive/2026-06-25-praxis-brief.CODEX-VARIANT.html` | `git show codex/claude-routine-2026-06-25-praxis:...` | Preserved divergent variant (NOT adopted) |

## Sensitive-data handling

- **Nothing sensitive was copied into this package.** This is a public static site; the sensitive
  surface is small (see `CURRENT_STATE.md §4`).
- Secrets (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`) are referenced **by location** (GitHub Actions
  secrets) only — never their values.
- Gitignored draft briefings (`skills-briefings-files/**/drafts/`) are referenced by path as the
  renderer's `content.json` inputs but not copied; they must stay untracked (they'd otherwise deploy).

## De-risk actions taken this pass (gating checklist)

1. **Recovered single-copy work:** committed the untracked `briefings/2026-07-01/praxis-brief.html`
   (a complete brief left uncommitted by a concurrent-run race) — commit `d12eb0d`.
2. **Preserved divergent branch content:** archived the `codex/*` divergent 06-24 / 06-25 briefing
   variants to `readiness/archive/` before they could be lost; recorded in the DO-NOT-TRUST blocklist.
3. **Ran + captured measurement artifacts:** tests, both generators, health-check → `EVIDENCE/`.
4. **Corrected a stale conclusion:** the deterministic renderer is shipped (all 7 types), not an
   in-progress pilot as a prior memory claimed — corrected in `CURRENT_STATE.md`/`EVOLUTION.md` and
   flagged in `TRUTH_TABLE.md`.
5. No footgun deploy scripts exist in-repo (deploy is GH Actions on push to main); none neutralized.

## Acceptance test (intent-reconstruction)

Two fresh-context cold reads (Sonnet, no prior conversation context):
- **BASELINE** — natural entry points only, `readiness/` excluded. Measures pre-package legibility.
- **ACCEPTANCE** — the `readiness/` package only, everything else excluded. Measures whether the
  package alone carries the intent.

Both runs completed 2026-07-02 (fresh Sonnet agents, no conversation context).

### BASELINE result summary (natural entry points, `readiness/` excluded)
Cold reader got **PURPOSE at file 1** — CLAUDE.md is "unusually good... accurate everywhere I could
cross-check." So base legibility for *what the project is* is already high. Gaps it hit:
- **Evolution: LOW-MEDIUM** — no history/changelog doc in the natural read set; only two dated inline
  notes in CLAUDE.md. Couldn't reconstruct the eras/pivots.
- **Working-vs-broken: unverified** — didn't run scripts, so "passing" was taken on faith; couldn't
  tell if the unpushed "Recover... praxis" commit meant a stuck deploy.
- **Skill-folder → output-filename mapping is undocumented** (`briefing-alpha` → which file?) — its
  single biggest "couldn't determine."
- Its decision-critical claim was the generic "generate-index warnings are blocking" rule (INHERITED),
  **not** the renderer CI-test gap — the messy repo does not surface the real load-bearing risk.

### ACCEPTANCE result summary (the `readiness/` package only)
Cold reader got **PURPOSE at file 1**, full confidence by file 3, and — critically — **independently
identified the exact decision-critical invariant** ("`render.test.js` does not run in CI") as the one
thing to act on, plus the concurrency race as the top open issue and the full 5-era evolution — none of
which the baseline surfaced. This is the package doing its job: it carries the *actionable* intent the
messy repo hides. Two package defects were caught and **both fixed**:
1. **`MODULE_MAP.md` contradicted `CURRENT_STATE.md`** on renderer status (map still said
   "in-progress/opt-in"). **Fixed** — the map now states the renderer shipped (all 7 types, authoring-
   time, correctly out of the deploy path) and reframes the CI-test gap as the real issue. A
   skill-folder→filename mapping table was also added (§6) to close the baseline's mapping gap.
2. **`READINESS_MANIFEST.md` appeared missing** — a timing artifact only: the acceptance agent launched
   before this file was written. It exists now; no action needed.

### Verdict
**PASS (after the one fix).** Delta is decisive: the package moves a cold reader from "I can tell what
this is but not what's wrong or how it evolved, and I can't verify anything" to "I know the one
load-bearing risk to act on, why, and how to re-derive it." The internal contradiction the test caught
has been resolved and the package re-checked for consistency. Fable can boot from `ONESHOT_PROMPT.md`.
